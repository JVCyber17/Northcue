# Overnight proving, 6 August 2026

The frozen release run whole, attacked, and drilled. Freeze held: no
feature, no refactor; two repo additions, both proving machinery (the
monitoring fire drill and the launch runbook), plus this record.

## Part 1, the launch-day rehearsal

The launched configuration ran whole in-process, launch flag flipped for
the rehearsal only. English over all 57 benchmark candidates: zero
rejections, the pinned 88 foreign-source repairs, and ZERO differences
between launched and today's stripping, so launch changes nothing for an
English reader, measured. All nine languages' real captured model output
(the 504-sample sweep, 8,000+ sentences) replayed through the wired
stripper: every command strip in every language came from that language's
measured caught set, and the nine reviewed banks produced zero strips
through the wired path, which is the provenance re-verification on the
live wiring.

**ro-31, corrected**: the expectation was a strip appearing once. The
answer key says guard_fires FALSE: ro-31 is the have-ready sentence,
outside the English 21, a measured keep. The rehearsal confirmed zero
strips of ro-31 anywhere, which is the correct count. The note that
predicted its strip was wrong, not the guard.

Rehearsal artifacts, named so they are not mistaken for findings: the
harness passed no exemption set, so the English phone rule stripped
engine-authored protected lines that production always exempts; and the
harness printed [undefined] for document ids through a field-name typo of
its own.

No local Postgres exists, so phase7_language.sql could not be applied to
a real scratch schema; its idempotence is by construction (add column if
not exists), and the service's retry-without-column tolerance is pinned
by test, so migration ordering cannot cost a session either way.

## Part 2, the adversarial night

Scam letters in all nine languages through the full launched path, corpus
where they exist and synthetic in the polish_phishing shape where they do
not: all nine refused verification_only with 2 to 6 signals, provider
skip verification_only_state in every language (no model prose can reach
a refused reader), and zero untranslated refusal strings anywhere.

The stacked repair (invented date + capitalised enums + impossible
notation in one result): not rejected, the invented date repaired away
and reaching no reader, enums normalised. Recorded, not fixed: the
impossible-notation token "2026.19.44" survives as prose because month 19
day 44 parses as no date at all; it cannot mislead as a date.

The timeout boundary at a 2-second drill ceiling through the real code
path with an abort-honouring provider stub: 1700ms completes with six
cards; 2600ms times out cleanly to ai_timeout with the six fallback cards
served. Concurrency: four simultaneous prose calls completed in 1546ms
wall against 6000ms serial, four distinct outputs, zero
cross-contamination, exactly as the step 2e load report modelled.

Per-language guard injection was already pinned by tests/guardWiring
(33 tests) and re-proven over the sweep in Part 1.

## Part 3, the monitoring fire drill (committed as a permanent test)

Every alarm fired against synthetic rows through the real script: the
per-language zero-completion page with exit 1 and the language named; the
one-session watch line that does not page; unknown-language history that
can never page; the global zero-completion page; and the digit drill
through the real row writer, shape-only asserted. Recorded boundary: the
per-language alarm is the zero-completion floor; a proportional drop
prints and is read, not paged.

## Part 4, the launch runbook (committed)

docs/LAUNCH_RUNBOOK.md, one page: migration, one-line flag commit,
deploy, what healthy looks like in the first hour and first day, the two
failure signatures (completion drop, refusal anomaly), the one-line
rollback.

## Part 5, the ingest pre-flight

Nine synthetic packs, each shaped from its language's real answer key,
through the real ingest: eight clean paths verified true; Romanian
rehearsed both disagreement flows end to end, ro-14 as the blocking
wired-guard over-fire with proposed exception and founder checkbox, and
ro-31 as the recall candidate with the mirror-boundary proposal, verdict
correctly refusing verified while the blocking defect stands.

## What was fixed tonight

Nothing needed fixing. Two observations recorded above (the impossible
notation token; the proportional-drop boundary), neither release-blocking,
both pack-independent.

## The readiness table

    lang   status      detail
    gu     pending     pack with founder
    hi     pending     pack built, founder designated, not yet handed
    pl     pending     pack built, reader needed
    ro     pending     pack built, reader needed
    es     pending     pack built, reader needed
    fr     pending     pack built, reader needed
    pt     pending     pack built, reader needed
    bn     pending     pack built, reader needed
    pa     pending     pack built, reader needed

## The remaining list

Nine packs. The founder's word. The migration. The flag.
